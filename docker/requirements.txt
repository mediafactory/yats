# ⚠ Shared deps MUST be kept in lockstep with vagrant/requirements.txt
#   (this file only adds gunicorn + load_dotenv at the end).
# Pinned for Django 5.2 LTS / Python 3.12 (migration June 2026).
Django>=5.2,<5.3
diff-match-patch
Markdown
six
pillow
pydevd
itsdangerous
defusedxml>=0.7.1
pyclamd
dnspython>=2.6
vobject>=0.9.9
monotonic
SpeechRecognition>=3.16.1
django-markdownx>=4.0.11,<5
django-haystack>=3.3,<4
xapian-haystack>=4.0,<5
pygments
furl
rpc4django
# CalDAV: embedded Radicale 3.x WSGI app (see modules/dav/) — replaces the dead
# djradicale + Radicale 1.x stack.
radicale>=3.1
icalendar>=5.0
requests
django-background-tasks>=1.2.8
pydub
httplib2
pymemcache>=4.0
Wand>=0.7.1
gunicorn
load_dotenv
