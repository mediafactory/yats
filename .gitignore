*.log
*.pyc
*.mo
/distribution/
/files/
/sites/static/
.settings/
.project
.pydevproject
.DS_Store
*.pot
/sites/web/wiki/
/external/
/vagrant/.vagrant/

# Tailwind standalone CLI binary (downloaded per-host; the compiled
# modules/yats/static/tailwind.css IS committed)
/.bin/

# Environments
.env
.venv
.venv-verify
env/
venv/
ENV/
env.bak/
venv.bak/

.vscode/
# local Django 5.2 migration verification venv
.venv-verify/

# filled per-web YATS configs contain DB credentials — never commit (template is tracked)
deploy/etc-yats/*.ini

# local test environment (start.sh): keep test.ini/test_settings.py/requirements.txt,
# ignore the generated venv + SQLite/static/log data
/testenv/.venv/
/testenv/data/
